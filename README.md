# FusionForgePCs

FusionForgePCs is a web application for browsing PC components and building a PC configuration with compatibility checks.

## Setup

1. Install dependencies:
   npm install

2. Run in development:
   npm run dev

3. Build for production:
   npm run build

4. Start production server:
   npm run start
